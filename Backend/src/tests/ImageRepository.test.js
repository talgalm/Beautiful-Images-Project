const ImageRepository = require('../repositories/ImageRepository');
const { Image, FinalRating, TmpRating } = require('../Models');

describe('ImageRepository', () => {
  describe('generateSmalllImages', () => {
    it('should generate small images for all images in the database', async () => {
      // Test implementation here
    });
  });

  describe('generateSmallImage', () => {
    it('should generate a small image for the given image name and category', async () => {
      // Test implementation here
    });
  });

  describe('initializeImagesDB', () => {
    it('should initialize the images database', async () => {
      // Test implementation here
    });
  });

  describe('fetchImages', () => {
    it('should fetch all images for the given email', async () => {
      // Test implementation here
    });
  });

  describe('fetchNewImages', () => {
    it('should fetch new images for the given email', async () => {
      // Test implementation here
    });
  });

  describe('fetchImage', () => {
    it('should fetch the image with the given imageId', async () => {
      // Test implementation here
    });
  });

  describe('fetchSessionImages', () => {
    it('should fetch session images for the given email', async () => {
      // Test implementation here
    });
  });
});
